let guestList = [ "hina", "kanwal", "ayesha", "hira"];

//guestList.forEach(freind => console.log (`salam ${freind}, would you like to dinneer with me`));

let lengthGuests : number = guestList.length;
console.log(`We are inviting total ${lengthGuests} guest.`);